syntax = "proto3";

package stream;

option go_package = "grpc-stream-demo/streampb";

service StreamService {
  rpc ServerStream (StreamRequest) returns (stream StreamResponse);
}

message StreamRequest {
  string name = 1;
}

message StreamResponse {
  string message = 1;
}